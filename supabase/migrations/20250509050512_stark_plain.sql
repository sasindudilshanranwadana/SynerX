/*
  # Remove previous migration as it's no longer needed
  
  The user_id column is already text in the updated schema
*/

-- This migration is now empty since the changes are included in the previous migration